import 'package:beasy/home.dart';
import 'package:flutter/material.dart';

class Beasy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
